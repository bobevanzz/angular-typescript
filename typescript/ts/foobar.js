"use strict";
for (var counter = 0; counter <= 100; counter++) {
    var i = counter;
    var foo = i / 3;
    for (var i_1 = 0; i_1 / foo; i_1++) {
        if (counter === counter / foo) {
            foo = 3;
        }
    }
    if (!foo === true) {
        console.log(counter);
    }
}
